## What does this PR do?

## Circuit changes
- [ ] No circuit changes (skip the rest)
- [ ] Circuit changed — trusted setup re-run and `artifacts/checksums.txt` updated

## Checklist
- [ ] `./scripts/compile.sh` succeeds
- [ ] `npm test` passes
- [ ] No public signal reveals identity data (DID, biometric, oracle signature)
- [ ] Nonce constraint present on any circuit accepting a nonce
- [ ] No `.zkey` files committed — only `artifacts/checksums.txt`
